"""Jinja2 contract templates + metadata.

Templates in this package are demo scaffolds only. They MUST be
reviewed by a licensed Texas attorney before any version of Agentary
ships a signed contract to a real user. The ``last_reviewed_date``
field in the metadata YAML starts at ``PENDING-LEGAL`` and is the
authoritative gate enforced by ``pool_contract_builder``.
"""
