[project]
name = "agentary"
version = "0.1.0"
description = "AI research platform — opportunity scout, research engine, and voice assistant"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
